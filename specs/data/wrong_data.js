/*
 * Test if check_prerequisites fails with wrong data.
 */

var wrong_data = 'foo bar';

///////////////////////////////////////////////////////////////////////////////

/*
 * Test if check_prerequisites fails with incomplete data.
 */

var wrong_data_2 = [
	{ value: 'a', id: 1}
];
